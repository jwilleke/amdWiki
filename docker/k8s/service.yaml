# ngdpbase Kubernetes Service
# Deploy with: kubectl apply -f service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ngdpbase
  labels:
    app: ngdpbase
    app.kubernetes.io/name: ngdpbase
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: ngdpbase
---
# Optional: LoadBalancer service for direct external access
# Uncomment if not using Ingress
# apiVersion: v1
# kind: Service
# metadata:
#   name: ngdpbase-lb
#   labels:
#     app: ngdpbase
# spec:
#   type: LoadBalancer
#   ports:
#     - port: 80
#       targetPort: 3000
#       protocol: TCP
#       name: http
#   selector:
#     app: ngdpbase
