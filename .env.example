# Environment Configuration Template
# Copy this file to .env and fill in your values
# Never commit .env - it should only exist locally

# Project Information
PROJECT_NAME="amdwiki"
PROJECT_REPOSITORY="https://github.com/jwilleke/amdWiki"

# Application
NODE_ENV=development
# Please keep ports above 9000 to avoid conflicts with system services
PORT=9000

# Instance Data Folder
# All instance-specific data (pages, users, attachments, logs, search index, etc.)
# MUST be stored within this folder. This includes:
#   - pages/           Wiki content
#   - users/           User accounts
#   - attachments/     File attachments
#   - logs/            Application logs
#   - config/          Custom configuration (app-custom-config.json)
#   - search-index/    Search index
#   - backups/         Backup files
#   - sessions/        Session files
#
# Custom config priority:
#   1. ./config/app-custom-config.json (for development)
#   2. INSTANCE_DATA_FOLDER/config/app-custom-config.json (for Docker/K8s)
#
# Default: ./data (relative to project root)
# Example: /var/lib/amdwiki/data
INSTANCE_DATA_FOLDER=./data

# Add your environment variables below
# DATABASE_URL=
# API_KEY=
# JWT_SECRET=
