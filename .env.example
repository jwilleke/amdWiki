# Environment Configuration Template
# Copy this file to .env and fill in your values
# Never commit .env - it should only exist locally

# Project Information
PROJECT_NAME="amdwiki"
PROJECT_REPOSITORY="https://github.com/jwilleke/amdWiki"

# Application
NODE_ENV=development
# Default port for amdWiki (matches Docker/k8s configuration)
PORT=3000

# Instance Data Folder
# All instance-specific data (pages, users, attachments, logs, search index, etc.)
# MUST be stored within this folder. This includes:
#   - pages/           Wiki content
#   - users/           User accounts
#   - attachments/     File attachments
#   - logs/            Application logs
#   - config/          Custom configuration
#   - search-index/    Search index
#   - backups/         Backup files
#   - sessions/        Session files
#
# Configuration merge order:
#   1. config/app-default-config.json (read-only, in codebase)
#   2. INSTANCE_DATA_FOLDER/config/{INSTANCE_CONFIG_FILE} (instance overrides)
#
# Default: ./data (relative to project root)
# Docker/K8s: /app/data
INSTANCE_DATA_FOLDER=./data

# Instance Config File
# Name of the config file to load from INSTANCE_DATA_FOLDER/config/
# This allows using different config files for different deployments
# Default: app-custom-config.json
# INSTANCE_CONFIG_FILE=app-custom-config.json

# Add your environment variables below
# DATABASE_URL=
# API_KEY=
# JWT_SECRET=
